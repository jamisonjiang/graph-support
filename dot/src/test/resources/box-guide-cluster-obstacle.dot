digraph G {
  subgraph cluster_A {
    e;
    c;
    subgraph cluster_B {
      label="unrelated obstacle";
      subgraph { rank=same; e->g; }
    }
    subgraph { rank=same; a->b->c->d; }
    subgraph { c->e; d->f; e->f; }
    a->c;
    b->e;
    a->f [color="#ff0000"];
    a->g;
  }
}
